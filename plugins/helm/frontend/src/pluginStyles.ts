// generated-style.css is produced by `pnpm build:css` (Tailwind CLI) before
// tsup runs, and loaded here as raw text via esbuild's text loader (see
// tsup.config.ts) so it ships embedded inside dist/index.js.
import css from "./generated-style.css";

export const PLUGIN_STYLES = css;
